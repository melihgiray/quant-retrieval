# Checkpoint from epoch 2 of configs/base.yaml. All three epochs are evaluated
# on validation so the choice of checkpoint is visible rather than asserted.
run_name: minilm_tuned_epoch2_val
seed: 17
retriever: dense
split: val
max_results: 100
display_name: Tuned, batch 64, epoch 2
display_order: 33
parameters:
  model_name: checkpoints/minilm_tuned/epoch-2
  batch_size: 128
  max_length: 256
  device: auto
output: results/minilm_tuned_epoch2_val.json
